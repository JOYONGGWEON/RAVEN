# RAVEN 프로젝트 컨텍스트

> 이 파일은 claude.ai 웹 채팅에서 나눈 기획/설계 논의를 정리한 문서입니다.
> Claude Code는 프로젝트 폴더에 있는 이 파일을 세션 시작 시 자동으로 읽습니다.
> 새 기능 작업을 시작하기 전에 이 문서 전체를 먼저 참고하세요.

---

## 1. 프로젝트 개요

- **이름**: RAVEN (AI Stock Manager) — 국내/해외 주식 트레이딩 분석 웹앱
- **사용자 수준**: 코딩 초심자. 설명은 쉽게, 전문용어는 풀어서.
- **목표**: ChatGPT로 만든 기존 정적 웹앱(HTML/CSS/JS)을 개선 → 최종적으로는 앱(App) 형태까지 발전
- **핵심 철학**: "뉴스에 낚이는 앱"이 아니라 **실제 거래량, 수급(프로그램/신용/대차/공매도), 캔들·이평선 등 차트 움직임을 기반**으로 판단하는, 월가/전문 트레이더 방식의 분석 도구를 지향함

---

## 2. 기존 코드 현황 (v6.6 / v7.0, GitHub Pages 배포 중)

파일 구성: `index.html`, `style.css`(1447줄), `app.js`(2427줄)

### 현재 구조
- **화면 흐름**: PIN 입력(`0524`) → "GOOD DAY SIR" 인트로 → 티커 입력 → 분석 결과 화면
- **데이터 소스**: Yahoo Finance API를 `corsproxy.io`(무료 공개 프록시)를 통해 호출 — **완전한 클라이언트 사이드(백엔드 없음)**
- **매크로 대시보드**: 미국 10년물 금리, VIX, USD/KRW 환율, BTC
- **기술적 분석 엔진**: EMA(5/20/60/120), RSI(Wilder), MACD, 스윙 포인트 클러스터링 기반 지지/저항선, 캔들 패턴 12종 자동 인식, R:R 계산, 목표가/손절가, 포지션 사이즈 계산기
- **점수 시스템**: 여러 지표를 규칙 기반(if-else)으로 합산해 0~99점 + S/A/B/C/D 랭크 부여 (⚠️ "AI"라는 이름이지만 실제로는 진짜 LLM이 아니라 규칙 기반 로직임)
- **차트**: TradingView 무료 위젯 임베드
- **전략 요약**: 계산된 지표를 조합한 규칙 기반 자연어 코멘트 생성

### 발견된 주요 문제점 (개선 필요)
1. **PIN코드가 app.js에 평문으로 노출** — 개발자도구로 누구나 확인 가능. 보안 개선 필요 (서버 인증으로 이전)
2. **`corsproxy.io` 공개 프록시 의존** — 트래픽 제한/다운타임 위험. 자체 서버 구축 후 완전히 제거해야 함
3. **국내 주식(KOSPI/KOSDAQ) 미지원** — Yahoo Finance 티커만 가능. 원화 표시도 단순 환율 환산일 뿐 실제 국내 종목 조회 기능 없음
4. **프로그램매매/신용/대차/공매도/CFD 데이터 전무**
5. **뉴스/실적(재무제표) API 미연동** — 플레이스홀더 텍스트만 존재
6. **골든크로스/데드크로스 등 매수·매도 신호 감지 로직 없음** (이동평균 계산만 있고 이벤트 감지는 없음)
7. `generateDemoData()` 등 안 쓰는 죽은 코드 존재

---

## 3. 사용자가 요청한 신규 기능

1. **국내주식 지원**
2. **실제 AI 분석 연동** (현재는 규칙 기반 → 진짜 LLM 서술 분석으로 전환)
3. **실시간 매매 동향 및 수급 확인**: 프로그램매매, 신용거래, 대차거래, 공매도, CFD
4. **관심종목 매수/매도 타점 자동 판단 기능**: 거래량, 수급(위 4가지), 골든크로스/데드크로스 등 기반
5. **뉴스 + 실적 그래프**: 티커 검색 시 관련 뉴스, 과거 4분기 매출액/영업이익, 다가오는 실적발표 예상치 그래프

---

## 4. 데이터 현실성 검토 결과 (중요 — 기대치 조정 완료, 2026-07-14 갱신)

| 항목 | 결정된 처리 방식 |
|---|---|
| **국내+해외주식 시세/차트(OHLC)** | ✅ **KIS Developers API로 완전 통합** (2026-07-25, 토스 완전 제거 — 아래 "토스→KIS 전환" 항목 참고). 국내는 `inquire-price`/`inquire-daily-itemchartprice`, 해외는 거래소코드(NAS/NYS/AMS) 순차 탐색 + `overseas-price` 시세/일봉. 실계좌 기반이라 무료 데이터소스 특유의 15~20분 지연이 없음 |
| **환율(USD/KRW)** | ✅ **Yahoo Finance(`KRW=X`)로 전환** (2026-07-25, 토스 `/exchange-rate` 제거). 지연시세지만 환율은 장중 변동폭이 작아 영향 미미로 판단 |
| **매크로 지표(VIX, 미국 10년물, 유가)** | ⚠️ KIS는 증권사 API라 지수/원자재를 종목으로 취급 안 함 → **Yahoo Finance 계속 사용**. BTC는 2026-07-25에 유가(WTI, `CL=F`)로 교체됨(사용자 요청) |
| **프로그램매매, 공매도, 대차잔고, 신용잔고 (4종)** | ✅ **KIS Developers API로 최종 확정** (2026-07-15 재검토). KRX 정보데이터시스템은 리뉴얼된 "Data Marketplace"에서 로그인 필요로 막혀있음을 확인 → 대신 KIS 공식 문서에서 4종 전부 종목별/일별 전용 엔드포인트를 찾음: `GET /uapi/domestic-stock/v1/quotations/program-trade-by-stock-daily`(프로그램매매), `daily-short-sale`(공매도), `daily-credit-balance`(신용잔고), `daily-loan-trans`(대차거래). 전부 종목코드+조회기간 파라미터로 일자별 시계열 응답(과거 데이터 백필도 가능). 공매도 엔드포인트는 실전 TR ID(FHPST04830000)까지 확인 완료, 모의투자 미지원(실전 계좌 전용). D+1 공시 데이터라 **"전일 데이터 기반 해석 + 내일 예상" 방식**은 그대로 유지 |
| **CFD 수급** | ❌ **포기하기로 결정** (국내 CFD 취급 증권사 소수, 데이터 API 공개 안 됨) |
| **국내 재무제표(실적)** | DART(전자공시시스템) Open API — 무료 |
| **해외 재무제표/실적 컨센서스** | 기존 Yahoo Finance 활용 (무료 대안은 정확도 낮음 — 필요시 유료 데이터 검토) |

> (2026-07-25 갱신) 토스증권은 완전히 제거됨 — 시세/차트는 KIS, 환율/매크로는 Yahoo로 통합. **수급 4종(프로그램매매/공매도/대차/신용잔고)도 계속 KIS Developers API**. KRX 정보데이터시스템 직접 스크레이핑은 로그인 필요 이슈로 포기.

---

## 5. 확정된 아키텍처

```
[프론트엔드] RAVEN 웹앱 (기존 HTML/CSS/JS 개선)
        ↕ (API 호출)
[백엔드 서버] Node.js — 신규 구축
        ├─ KIS Developers API    → 국내+해외 개별 종목 시세/차트(OHLC) + 프로그램매매/공매도/대차/신용잔고 4종 (2026-07-25부터 시세/차트도 KIS로 통합, 토스 제거)
        ├─ Yahoo Finance         → 환율(USD/KRW) + 매크로 지표(VIX/미국 10년물/유가 — KIS가 지수·원자재 미지원). corsproxy.io는 완전 제거, 서버가 직접 호출
        ├─ DART Open API         → 국내 실적(분기 매출/영업이익)
        ├─ 뉴스 API              → 국내(네이버뉴스)/해외(NewsAPI 등)
        ├─ Claude API (Anthropic) → 실제 AI 서술 분석 (API 키는 서버에만 보관, 프론트 노출 금지)
        └─ DB (Supabase)         → 관심종목, 알림설정, 수급데이터 캐시
        ↕
[스케줄러] 정해진 시간마다 자동 실행 (예: 새벽 6시 수급데이터 수집, 5분마다 관심종목 체크)
        ↕
[알림] 조건 충족 시 → 텔레그램 봇으로 메시지 발송
```

### 확정된 기술 스택
| 영역 | 선택 | 이유 |
|---|---|---|
| 백엔드 | Node.js + Express | JS로 프론트/백엔드 통일, 초심자가 새 언어 안 배워도 됨 |
| 배포 | Render.com | 무료 티어, 24시간 상시 실행(실시간 감시 필수), 문서 친화적 |
| DB | Supabase (Postgres) | 대시보드 클릭 기반 관리, 무료 티어 넉넉함 |
| 실시간 알림 | 텔레그램 봇 | 이메일/웹푸시보다 구현 압도적으로 쉬움 |
| AI 분석 | Claude API | 서버에서 지표 요약 → 자연어 해석 생성 |
| 국내+해외 시세/차트 · 수급데이터 | KIS Developers API | 시세/차트/수급 4종 전부 실증 확인, IP 허용 등록 불필요(Render에서 바로 호출됨) — 2026-07-25에 토스 대체 |
| 환율/매크로 지표 | Yahoo Finance | KIS가 다루지 않는 환율·지수·원자재 전용, IP 제한 없음 |

---

## 6. 전체 로드맵 (단계별 — 순서대로 진행)

### Phase 0. 준비 (1회성 세팅) — ✅ 완료 (2026-07-14)
- [x] ~~KIS Developers 가입~~ → **토스증권 Open API**로 대체 발급 (Client Id/Secret 확보, 로컬 `open api.txt`에 보관·git 제외). 시세/차트 지원 확인 완료 (아래 4번 문서 참고), 프로그램매매는 KRX로 이관 결정
- [x] 텔레그램 봇 생성 (BotFather) — Bot Token / Chat ID 확보
- [x] GitHub 저장소 정리 — `github.com/JOYONGGWEON/RAVEN` 신규 생성, 초기 커밋 push 완료
- [x] Render.com, Supabase 가입 완료

### Phase 1. 백엔드 전환 + 국내주식 기반 다지기 — ✅ 완료 (2026-07-14)
- [x] Node.js 서버 뼈대 생성 (`server/` — Express, `server/src/index.js`)
- [x] `corsproxy.io` 의존 완전 제거 → 서버가 직접 Yahoo 호출 (`/api/yahoo/chart`). 브라우저 실검증 완료(매크로 대시보드 4개 지표 정상 수신). ※ `/api/yahoo/profile`은 2026-07-20에 죽은 코드로 확인되어 라우트째 제거함(아래 참고)
- [x] 국내 종목 시세 + 차트(OHLC) 조회 연동 — **토스증권 Open API** (`server/src/lib/tossAuth.js` OAuth2 토큰 캐싱, `server/src/routes/toss.js` `/api/toss/prices`, `/api/toss/candles`). 삼성전자(005930) 실데이터로 검증 완료
- [x] **개별 종목 데이터 국내+해외 전부 토스로 통합** — AAPL 등 해외 티커도 토스 `/candles`로 정상 조회됨을 확인하고 Yahoo 기반 해외 조회 경로(`fetchOverseasStockData`)를 완전히 제거. 환율(USD/KRW)도 Yahoo KRW=X → 토스 `/api/toss/exchange-rate`로 전환. Yahoo는 매크로 지표(VIX/10년물/BTC)만 남음
- [x] 프론트에 국내/해외 자동 판별 로직 추가 (`isDomesticTicker` — 통화 표시(₩/$) 및 회사프로필 조회 스킵 여부 판단용으로 사용, 데이터 소스 자체는 국내/해외 모두 토스로 통일됨). 브라우저 실검증 완료(005930, AAPL 둘 다 정상)
- [x] PIN 인증을 서버 사이드로 이전 (`POST /api/auth/verify-pin`, IP당 5회 실패 시 60초 잠금). 브라우저 실검증 완료
- [x] (로드맵 외 추가) 국내 종목명 검색/자동완성 — 토스 API가 이름 검색 미지원이라 KRX KIND 공개 상장법인목록으로 이름↔코드 매핑 자체 구축(`server/src/lib/stockDirectory.js`). 결과 화면 타이틀도 "삼성전자 (005930)" 형태로 표시

> ⚠️ Yahoo `quoteSummary`(회사 프로필/섹터) 엔드포인트는 Yahoo 측에서 크럼/쿠키 인증을 요구하도록 바뀌어 401 발생 — chart 엔드포인트는 정상. 원래도 실패 시 null 처리되던 부분이라 앱 동작에는 영향 없음. 필요 시 별도 해결 필요. 국내 종목은 애초에 프로필 조회를 스킵하도록 처리함.
> ⚠️ 국내 종목 테스트 중 발견한 통화 표시 버그 수정함 — 원화 가격을 달러 포맷터에 넣고 환율을 또 곱해 완전히 잘못된 숫자가 나오던 문제. `formatPrice()`(국내=₩, 해외=$) 도입으로 해결.
> ✅ 포지션 사이즈 계산기 완료 — 원래 CALC 버튼에 로직이 전혀 연결 안 되어 있었음(발견 후 구현). 국내(₩)/해외($) 통화 자동 전환, 진입가 자동입력 지원.
> ✅ TradingView 국내 종목 에러 처리 완료 — `KRX:005930` 형식이 맞는데도(카카오 등 다른 종목으로도 재현) 무료 위젯이 KRX 실시간 데이터 재배포를 막아둔 것으로 확인(라이선스 제약, 심볼 포맷 문제 아님). 위젯 호출 자체를 생략하고 "차트 준비 중" 안내로 대체. 결과 화면 타이틀은 국내/해외 모두 종목명만 표시(코드/티커 없이 — 예: "삼성전자", "애플") 하도록 최종 확정됨.
> 📌 후속 과제(별도 트래킹): 국내 종목 자체 차트(`lightweight-charts` + 토스 캔들 데이터) — Phase 2 이후 후순위.
> 📌 실제 배포 시 주의: 토스 WTS에서 "허용 IP 등록" 필요 — Render 배포 후 서버 고정 IP를 등록해야 API 호출 가능 (로컬 테스트는 이미 등록된 IP라 통과됨).
> ✅ (로드맵 외 추가, 2026-07-16) 분석 결과 화면 UI 리디자인 — 요약(전략요약+목표가/손절가+포지션계산기)을 항상 보이게 승격하고, 나머지 상세는 추세·모멘텀/수급/패턴·신호/실적 4개 탭(슬라이드 전환)으로 재구성. "Market Regime" 빈 섹션·"Single Stock" 무의미 태그 삭제. "Z SCORE/Z RANK"를 "RAVEN SCORE/등급"으로 개명하고 무엇의 합산인지 캡션 추가. 매수/관망/매도 판정을 rank 단독 판단(이분법)에서 R:R 기준 3단계로 통일. 팩터 뱃지(Trend/Momentum/Vol/R:R) 색상 코딩 적용(기존 CSS는 있었으나 미연결 상태였음).
> ✅ (로드맵 외 추가, 2026-07-19) 디자인 2차 정리 — 티커명 아래로 가격 이동(흰색) + 등락률(▲/▼) 표시 추가(긴 종목명 줄바꿈 문제 해결). Risk/FX/Crypto pill 행 전체 삭제(완전 죽은 코드였던 `updateRegimePills()`도 같이 제거). "등급"→"RAVEN RANK"로 통일. Trend/Momentum/Vol 팩터뱃지 삭제(R:R만 유지). MA 20/60 지표박스 삭제, RSI에 정성 해석 태그 추가(과열/과매도/약세 등 — MACD는 이미 있었음). 전략요약↔목표가 박스 사이 CSS 겹침 버그 수정(margin 누락). 포지션 계산기를 탭 아래 최하단으로 이동. 탭 콘텐츠 제목 중복 표기 정리("Trend (추세)"→"추세" 등).
> ✅ (로드맵 외 추가, 2026-07-19) 분석 텍스트 엔진 검토 완료 — "전면 재작성" 요청으로 시작했으나, 실제로 코드를 다 읽어보니 Wave/Signal/전략요약의 규칙기반 분기(지지·저항 근접도 + R:R + RSI 상태머신)는 이미 탄탄해서 재작성 불필요로 판단. 대신 진짜 문제 3가지만 수정: ①죽은 코드 제거(`CANDLE_PATTERNS`/`SIGNAL_MAP` 상수, `news-txt` 블록 — 전부 선언만 되고 미사용) ②Wave·Signal·전략요약 세 곳이 "지지선 근처=매수/저항선 근처=리스크" 판단을 각자 중복 계산하던 것을 정리 — Wave는 순수 구조 설명으로, 매수/매도 판단은 전략요약이 전담하도록 역할 분리 ③캔들패턴 13종→18종으로 확대(관통형/먹구름형/스피닝탑/집게형 상·하단 추가).
> ✅ (로드맵 외 추가, 2026-07-20) **분석 엔진 퀀트 업그레이드 — "GPT 엔진 vs Claude 엔진" 전체 재검토 완료.** 코드를 처음부터 다시 읽고, 월가 퀀트 트레이더 관점에서 부족한 부분을 직접 진단해서 4단계로 진행함 (모든 항목 실제 API로 AAPL/NVDA/005930 실데이터 검증 완료, 커밋 `23b3d66`):
>   - **버그 수정**: 매수/매도 판정이 배지·전략요약·시나리오박스(`buildScenarios`) 세 곳에서 각자 다른 R:R 기준으로 계산되어 화면에 서로 모순되는 문구가 동시에 뜨던 버그 발견 → `computeVerdict()` 하나로 통일. `buildScenarios()`(계산만 하고 렌더링에 전혀 안 쓰이던 죽은 함수), `generateDemoData()`, Yahoo 프로필 조회(401로 죽어있던 기능, 서버 라우트까지 제거) 삭제. 급등일에 "저항선"이 오늘 자신의 고점으로 잡혀 목표가가 현재가보다 낮게 나오던 실제 버그도 발견·수정.
>   - **퀀트 핵심 지표 신규 추가**: ATR(14) 기반 손절/목표가(예전엔 지지선 없으면 변동성 무관 고정 -5%였음), MACD 시그널선(9-EMA)+히스토그램+골든/데드크로스 감지(예전엔 크로스오버 감지 자체가 불가능한 구조), ADX(14)+DI±로 추세 강도 측정(예전엔 MA20/60 격차로만 추정).
>   - **심화**: 지수 대비 상대강도(RS) — 국내는 KODEX200(069500), 해외는 SPY 벤치마크로 20일/60일 비교(개별 종목만 보고 시장 전체 하락을 놓치는 문제 방지). OBV 다이버전스 — 최근 10일 누적거래량과 가격의 괴리로 매집/분산 감지(예전엔 오늘 캔들 하나만 보고 수급 판단). 스윙 감지 3봉→5봉 피벗으로 노이즈 축소, 지지/저항 클러스터링 허용오차를 고정 3%→ATR 기반 적응형으로 전환.
>   - **RAVEN SCORE 재설계**: 위에서 새로 만든 ADX(추세강도)·MACD 크로스오버·RS를 점수 산정에 반영 (각 항목에 "왜 넣었는지" 주석 포함, 완전한 통계적 백테스트 기반 재설계는 아니고 근거 있는 규칙 기반 보완).
>   - 벤치마크(RS) 데이터 조회를 메인 종목 조회와 병렬로 가져오도록 `runAnalysisForTicker`의 `Promise.all`에 포함시킴 — RS가 이제 스코어 계산에 직접 들어가서 렌더링 전에 준비돼 있어야 함.
>   - 📌 후속 과제로 보류한 항목(이번엔 미착수): RAVEN SCORE 가중치의 실제 백테스트 기반 재조정(현재는 근거는 있으나 과거 수익률로 검증되지 않은 규칙 기반 가중치), S/A/B/C/D 등급 경계값 재보정.

### Phase 2. 수급 데이터 파이프라인 — ✅ 완료 (2026-07-15 ~ 2026-07-19)
- [x] KIS Developers 가입 및 API 키 발급 (수급데이터 전용 — Toss와 별개 계정/키)
- [x] KIS API 서버 연동 — `server/src/lib/kisAuth.js`(OAuth2 토큰) + `server/src/routes/kis.js` (`/api/kis/program-trade`, `/short-sale`, `/credit-balance`, `/loan-trans`). 삼성전자(005930) 실데이터로 4종 전부 검증 완료 (일자별 시계열 정상 수신)
- [x] Supabase DB 스키마 설계 + 연결 — `supply_demand_daily` 테이블(symbol/trade_date/data_type/raw_data JSONB, RLS 활성화). insert/select/delete 실제 검증 완료
- [x] 매일 새벽 자동 수집 스케줄러 — `server/src/scheduler.js` (node-cron, 매일 06:00 KST). 관심종목 기능이 아직 없어서 임시로 삼성전자(005930) 고정 목록, Phase 3에서 DB 기반 관심종목으로 교체 예정. `POST /api/kis/collect-now`로 수동 트리거 가능(테스트용)
- [x] "전일 수급 → 오늘 해석 + 내일 예상" 코멘트 로직 — `server/src/lib/supplyDemandInterpreter.js` (규칙기반). 캐시 없는 종목은 그 자리에서 KIS 즉시조회+캐싱하는 폴백 포함, 어떤 국내 티커든 동작. `GET /api/kis/supply-demand`로 노출, 결과화면 "수급" 탭에 연결 완료(해외 종목은 박스 자체를 숨김). Claude API 서술 결합은 Phase 4(진짜 AI 분석)에서 진행 예정 — 지금은 순수 규칙기반 문장

> ⚠️ KIS API 운영 시 주의: ①토큰 발급은 1분당 1회 제한 — 서버가 자주 재시작되면 걸릴 수 있음(정상 운영 중엔 문제 없음, 캐시된 토큰 재사용). ②데이터 조회는 "초당 거래건수" 제한이 빡빡해서 4종을 연속 호출하면 걸릴 수 있음 — 호출 사이 600ms 딜레이 + 1회 재시도로 대응. ③KIS는 HTTP 200이어도 응답 바디의 `rt_cd`로 성공/실패를 알려줘서, 이걸 체크 안 하면 레이트리밋 에러가 "0건 수집"으로 조용히 묻힘 (실제로 이 버그를 겪고 수정함).
- [x] 프론트 결과 화면의 "수급" 탭 — 기존 거래량 기반 추정 로직(Supply 박스)은 그대로 두고, 그 아래 실제 KIS 수급데이터 해석 박스를 추가하는 방식으로 결합 (완전 교체가 아니라 병기)

> ⚠️ KRX 정보데이터시스템 직접 스크레이핑 방식은 폐기 — "KRX Data Marketplace" 리뉴얼 후 로그인 필요로 확인되어(2026-07-15), 대신 KIS Developers의 종목별/일별 전용 엔드포인트로 대체 확정.

---

## 📌 다음 세션 시작 시 우선 처리

1. **배포 (모바일 접속용) — ✅ 완전히 완료 (2026-07-25)**
   - [x] 백엔드(`server/`) → Render.com 배포 완료. `render.yaml`(repo 루트)로 Blueprint 배포, 서비스명 `raven-backend`, URL: `https://raven-backend-z5uc.onrender.com`. `/health`, Supabase(`/api/watchlist`), KIS(`/api/kis/program-trade`) 전부 실제 호출 검증 완료(정상 응답).
   - [x] **프론트엔드 GitHub Pages — 이미 배포되어 있었음을 발견함(2026-07-23)!** 확인해보니 저장소가 (메모리에 "private"로 잘못 기록돼 있던 것과 달리) 실제로는 **public**이고, GitHub Pages도 이미 활성화되어 `https://joyonggweon.github.io/RAVEN/`에서 라이브로 서빙 중이었음(레포 루트 정적 서빙, main 브랜치 푸시마다 자동 반영 확인됨 — `curl`로 최신 커밋의 `/api/ai/analyze` 코드까지 실제 존재 확인). 언제/어떻게 켜졌는지는 불명(사용자가 이전에 직접 했거나 GitHub 기본 동작으로 추정) — 아무튼 새로 설정할 필요 없음.
   - [x] `app.js`의 `API_BASE`를 로컬 고정값에서 **호스트 자동 분기**로 교체 — `window.location.hostname`이 localhost/127.0.0.1이면 로컬 백엔드(`:3001`), 그 외(GitHub Pages 등)면 Render 백엔드(`https://raven-backend-z5uc.onrender.com`) 자동 사용. 정적 사이트라 빌드타임 환경변수 주입이 없어서 이 방식으로 처리함.
   - [x] 백엔드 CORS(`server/src/index.js`)를 **콤마 구분 다중 origin 지원**으로 변경 — 기존엔 `FRONTEND_ORIGIN` 문자열 하나만 허용해서 로컬(`localhost:5500`)과 배포 주소를 동시에 못 받았음. 로컬에서 두 origin(`http://localhost:5500`, `https://joyonggweon.github.io`) 모두 허용/차단 정상 동작 실제 검증 완료(curl로 OPTIONS 프리플라이트 테스트, 제3의 origin은 정상 차단됨 확인).
   - [x] Render `FRONTEND_ORIGIN` 환경변수 갱신 완료(사용자 직접) — curl로 재검증: GitHub Pages origin은 204 허용(`access-control-allow-origin` 정상 반환), 제3 origin은 차단됨.
   - [x] **Oracle Cloud 시도 → 최종 포기 결정 (2026-07-25)**: "Always Free" VM에 경량 프록시(tinyproxy/squid)를 얹어 토스 호출만 고정 IP로 우회시키는 방법을 여러 세션에 걸쳐 시도했으나 가입 단계("등록을 완료할 수 없습니다") 자체를 못 넘음. 이번 세션에 실제로 시도해본 것들 전부 실패: DCC(해외원화결제) 차단, 카드 변경, 브라우저 변경. 사용자가 명시적으로 "못하겠다, 지쳐서"라며 중단 선언 — **더 이상 오라클 재시도하지 말 것**.
   - [x] **토스→KIS/Yahoo 전환 완료 (2026-07-25, 같은 세션)**: Oracle을 포기하면서 "KIS로 통합하면 토스처럼 IP 등록 문제가 또 나오지 않냐"는 질문에서 시작 — 실측(Yahoo Finance가 `.KS`/`.KQ` 접미사로 국내 종목도 되는지, KIS 해외 시세가 실제로 되는지 등) 먼저 해보고 진행하기로 함. 최종적으로 사용자가 "KIS로 통합 + 토큰 저장방식 개선, Yahoo는 지연 무관 데이터(환율/VIX/유가)만" 방향으로 결정:
     - **시세/차트**: 국내+해외 전부 KIS Developers로 통합, 토스 완전 제거(`tossAuth.js`/`routes/toss.js` 삭제, `TOSS_CLIENT_ID/SECRET` 정리). 신규 `server/src/lib/kisMarket.js` — 국내는 `inquire-price`(현재가)/`inquire-daily-itemchartprice`(일봉), 해외는 거래소코드(NAS→NYS→AMS 순차 탐색, 성공한 코드는 심볼별로 캐싱) + `overseas-price`(현재가)/`dailyprice`(일봉). ⚠️ **실측으로 발견한 제약**: 국내/해외 일봉 둘 다 1회 호출당 최신 100개로 캡됨(요청 범위를 넓혀도 무시됨) — EMA120 등 워밍업에 필요한 180개 확보하려면 종료일(국내: `FID_INPUT_DATE_2`)/커서(해외: `BYMD`)를 과거로 옮겨 2회 호출 후 이어붙여야 함(둘 다 실측 검증 완료). 해외는 틀린 거래소코드를 넣어도 `rt_cd=0`(성공)에 필드만 전부 빈 문자열로 와서, `output.last` 존재 여부로 거래소를 판별해야 함.
     - **환율(USD/KRW)**: Yahoo `KRW=X`로 전환(`/api/yahoo/exchange-rate` 신설). 장중 지연시세지만 환율은 변동폭이 작아 실사용 영향 미미로 판단하고 진행.
     - **매크로 대시보드**: BTC 제거, 유가(WTI, `CL=F`) 추가(사용자 요청) — VIX/미국10년물은 그대로 Yahoo 유지.
     - **KIS 토큰 영구저장**: `kisAuth.js`가 메모리 캐시 → Supabase(`kis_oauth_token` 테이블, `db/schema.sql`에 추가) 순으로 재사용하도록 변경. 사용자가 Supabase SQL 에디터에서 테이블 생성 완료(2026-07-25).
     - **실전에서 발견한 버그**: 종목 검색 시 본종목+RS벤치마크(KODEX200/SPY) 캔들을 동시에 요청하는데, 토큰이 없는 상태에서 두 요청이 동시에 "새로 발급" 분기를 타면서 KIS "1분당1회" 제한에 걸려 하나가 실패하는 걸 브라우저 실테스트로 재현함 → `kisAuth.js`에 `inFlightFetch` 프로미스 락 추가해서 동시 요청이 토큰 발급 하나를 공유하도록 수정, 재검증 완료.
     - **분봉(1분봉) 초단기 보조지표는 미구현으로 남김** — KIS 분봉 엔드포인트가 미검증이라 서버가 일부러 400을 줘서 기존 soft-fail(실패해도 메인 분석엔 영향 없음) 경로를 그대로 타게 함. 필요해지면 별도 작업.
     - **해외 종목 한글명 표시도 보류** — 토스 종목마스터 같은 간단한 이름조회가 KIS엔 없어서, 해외 종목은 이제 한글명 대신 티커 그대로 표시(예: "애플" → "AAPL"). `updateUI()`의 기존 폴백이 처리하므로 에러는 아니고 UX만 소폭 후퇴.
     - 브라우저로 005930(코스피)/058610(코스닥)/AAPL(해외) 전부 실검색해서 캔들·RS벤치마크·지지저항·수급탭·환율변환까지 정상 렌더링 확인함.
   - [x] **커밋 + push + 프로덕션 검증 완료 (2026-07-25, 커밋 `4e567b6`)** — GitHub push로 Render/GitHub Pages 자동 재배포됨. 배포 후 실제 프로덕션 URL(`raven-backend-z5uc.onrender.com`)에 curl로 `/api/kis/candles`(삼성전자 실데이터 응답), `/api/yahoo/exchange-rate`(₩1,462.1 응답) 재검증 완료.

2. **RAVEN 분석 엔진 전체 재검토 — ✅ 완료 (2026-07-20)**
   - GPT 엔진과 비교해서 Claude 기준으로 판단 기준 자체(시그널/지지저항/캔들·보조지표/SCORE·RANK)를 재검토하는 작업. 아래 "Phase 1.5 — 분석 엔진 퀀트 업그레이드" 섹션에 상세 기록.

3. **Phase 3(매수/매도 시그널+관심종목+텔레그램 알림) — ✅ 완료 (2026-07-20)**
   - 상세는 아래 "Phase 3" 섹션 참고. 남은 건 실제 텔레그램 전송 테스트(동의 필요)와 관심종목 실사용 등록뿐, 코드/DB는 전부 준비됨.

4. **Phase 4(Claude API 실제 AI 서술 분석 연동) — ✅ 완료 (2026-07-23)**
   - 상세는 아래 "Phase 4" 섹션 참고. 실제 API 키로 종단 검증 완료.

5. **디자인 다듬기 — ✅ 완료 (2026-07-23)**
   - 이번에 추가한 지표(ATR/MACD/ADX/RS/OBV) 중 RS(지수 대비 상대강도)만 추세 박스 하단의 작은 보조 텍스트(`.section-subnote`)로 묻혀 있었음 → RSI/MACD/ADX/ATR과 동일한 지표 박스로 승격, `indicator-row`를 4열→5열 그리드로 확장. OBV는 이미 전용 박스가 있어 그대로 유지.

6. **엔진 정밀 감사 2라운드 — ✅ 완료 (2026-07-23, 같은 날 추가 세션)**
   - 사용자가 삼성전자/삼성전기 실데이터로 세부 피드백(수급·신호 탭 중복, 지지·저항 과대, 패턴 사각지대, 지표 줄바꿈, MACD 다이버전스 요청)을 줌 → 반영 완료:
     - 지지·저항 근접도 제한(±12~30%, ATR 기반) 적용 — 스코어 기반 클러스터링 픽 함수만 고침
     - 수급(supply-txt)/신호(signal-txt) 탭 중복 서술 제거, "이벤트/맥락 측면" 모호한 문구 삭제(특이 세션에만 신호탭에 📰로 짧게)
     - 캔들패턴: Doji 다음날 확인캔들(레인지를 상당폭 뛰어넘는 강한 봉) 반전확인 패턴 신규 추가 — Engulfing 조건(전일 몸통≥20%)이 Doji(몸통<10%)를 원천 배제해서 못 잡던 사각지대였음
     - MACD 다이버전스 감지 신규 추가(OBV와 같은 방식) — 모멘텀 카드 + 추세 종합에 반영
     - 지표박스 값 줄을 "값" + "▶ 해석" 두 줄로 분리(요청사항)
   - **2차 감사에서 자체적으로 찾아 추가한 것(요청 안 받음)**: ①ADX가 스냅샷 값만 보여주고 강도 강화/약화 추세는 안 보여주던 것 → `adxSeries` 추가해서 "강도 강화 중/약화 중" 표시 ②20일 변동성(volatility)이 RAVEN SCORE엔 반영되는데 화면 어디에도 안 보이던 "숨은 채점 요소" → ATR 박스에 노출
   - **곧바로 사용자가 재확인해서 발견한 진짜 버그**: 위 지지·저항 근접도 제한을 클러스터링 픽 함수에만 걸어놓고, 그게 실패했을 때 쓰는 60봉 fallback(`recentHighs`/`recentLows`, 근접도 제한 없이 단순 min/max)엔 안 걸어놔서 삼성전기(009150)에서 저항선이 현재가 대비 +64% 떨어진 값(₩2,417,000)으로 잡히는 게 실제로 재현됨 — target1(₩2,404,915)이 정확히 그 버그값과 일치하는 것까지 Node 스크립트로 재현·확인 후 fallback에도 동일한 거리 제한을 적용해 수정함. **교훈: 같은 값을 만드는 코드 경로가 여러 개면(클러스터링 + fallback) 전부 찾아서 고쳐야 함.**
   - 텔레그램 알림기(`server/src/lib/signalDetector.js`)는 지지/저항 로직 자체가 없어 이번 버그와 무관함을 확인(수정 안 함).
   - AI 서술 분석 관련 사용자 우려(뉴스/실적/기관수급 없이 그냥 지표를 문장으로 풀어주는 느낌)에 답변: 현재 AI는 이미 계산된 기술 지표만 서술하도록 의도적으로 설계됨(할루시네이션 방지 위해 새 숫자·뉴스를 지어내지 말라고 시스템 프롬프트에 명시) — 진짜 뉴스/실적/기관수급 서술은 Phase 5+에서 실제 데이터 소스를 붙인 뒤에나 타당하다고 설명, 지금 프롬프트를 풀어서 그럴듯한 가짜 뉴스를 만들게 하지 않기로 함.
   - **투자자별(개인/외국인/기관) 매매동향 — KIS API로 가능함을 실제 호출로 확인함**: `GET /uapi/domestic-stock/v1/quotations/inquire-investor` (TR_ID `FHKST01010900`)을 임시 테스트 라우트로 실제 호출 → `rt_cd:"0"` 정상 응답, 개인/외국인/기관 각각의 순매수 수량·순매수 대금·매수/매도 거래량을 일자별(30일치) 시계열로 제공함 확인. 기존 4종(프로그램매매/공매도/신용/대차)과 같은 KIS 계정 그대로 사용 가능. 확인 후 테스트 라우트는 제거함 — 사용자가 "다음 업데이트에 반영"하기로 명시적으로 예약(아래 우선순위 참고).

7. **수급 탭에 개인/외국인/기관 매매동향 추가 — ✅ 완료 (2026-07-25, 같은 세션)**
   - KIS `inquire-investor`(TR `FHKST01010900`)를 5번째 수급 데이터타입(`investor_trend`)으로 추가. 실측으로 정확한 필드명 확인(`prsn/frgn/orgn_ntby_qty` 등). `supplyDemandCollector.js`(수집) + `supplyDemandInterpreter.js`(해석 — 외국인·기관 동반 매도인데 개인만 받아내는 구조면 부담 요인, 동반 매수면 우호적 신호로 톤 판단)에 반영, 수급탭 제목/요약 문구도 "투자자별" 포함하도록 갱신.
   - ⚠️ **DB 마이그레이션 필요**: `supply_demand_daily`의 `data_type` CHECK 제약이 기존 4종만 허용해서, 새 타입 저장 시도 시 실제로 `violates check constraint "supply_demand_daily_data_type_check"` 에러가 나는 것까지 실측 확인함. `server/db/schema.sql`에 `alter table ... drop/add constraint` 추가해둠 — **사용자가 Supabase SQL 에디터에서 아직 안 돌림(미완료)**. 안 돌려도 앱은 안 깨지고 "투자자별 매매동향 데이터가 없습니다"로 우아하게 표시됨(브라우저 실검증 완료) — 돌리면 바로 실제 데이터로 채워짐.

8. **UI/UX 다듬기 + 실버그 수정 라운드 — ✅ 완료 (2026-07-25, 같은 세션)**
   - **미국 10년물 계산 버그 수정**: Yahoo `^TNX`가 이미 실제 금리(%)를 그대로 주는데(실측: 4.679=4.679%) 코드가 `/10`을 한 번 더 해서 0.47%처럼 잘못 표시되던 실제 버그 발견·수정. VIX/환율/유가 임계값은 재검토 결과 이상 없어 그대로 둠.
   - **RAVEN SCORE/RANK UI 통합**: 별도였던 두 배지를 "RAVEN SCORE" 하나로 합치고 `A+ 92점` 형태(+/-는 `<sup>` 위첨자)로 표시. `formatRankGrade()`가 기존 S/A/B/C/D 밴드 안에서 점수 위치(하위 1/3="-", 중위 1/3="", 상위 1/3="+")로 세부등급을 매김. 캡션 문구도 실제 스코어 계산 입력값(추세/모멘텀/변동성/R:R/RS)에 맞게 갱신.
   - **RS(상대강도) 표기 형식 변경**: "20일 -3.3%p / 60일 +4.6%p" → "20일/60일 = -3.3/+4.6(%p)"로 바꿔 좁은 화면에서 단어 중간에 어중간하게 줄바꿈되던 문제 해결.
   - **AI 분석요청 모바일 에러의 진짜 원인 발견**: 모바일 전용 버그가 아니라 **배포 서버(Render)에 `ANTHROPIC_API_KEY`가 아예 설정 안 되어 있던 것**이었음 — 실제 프로덕션 엔드포인트에 직접 curl 요청해서 정확한 원인 문구(`ANTHROPIC_API_KEY 환경변수가 설정되지 않았습니다`)까지 확인. 데스크탑 테스트는 로컬 서버(.env에 키 있음)로 붙어서 몰랐던 것. `render.yaml`에 해당 env var 항목 자체가 누락되어 있어서 추가함 — **실제 키 값은 사용자가 Render 대시보드에서 등록해야 함(미완료, 아래 참고)**.
   - **인트로 인사말 교체**: "GOOD DAY SIR" → 자비스 톤으로 "AT YOUR SERVICE, SIR" 시도했다가, 사용자가 곧바로 "GOOD DAY TODAY :)" / "RAVEN is online"으로 최종 확정(같은 세션, 9번 항목 참고).
   - **모바일 줌인 버그 수정**: 헤더 검색창(`.ticker-form #ticker-input`)의 모바일 브레이크포인트 `font-size`가 0.9rem(14.4px)로 16px 미만이라 iOS Safari가 포커스 시 자동 확대하던 게 원인 — 16px로 고정. `#entry-ticker`도 안전마진 차원에서 16px로 고정.
   - **원형 그래픽(raven-smoke-orb) 리디자인**: 기존 시안/핑크 2색 대비를 까마귀 깃털 특유의 무지개빛 광택(iridescence) 느낌의 청록→인디고→보라 다단 그라디언트로 교체, 링 안쪽 빈 공간(하단)에 작은 까마귀 실루엣(인라인 SVG, 고정 워터마크로 은은한 날갯짓 애니메이션)을 추가해 "까마귀"라는 이름을 시각적으로 상징화. 모바일/데스크탑 둘 다 스크린샷으로 실확인 완료.
     - ⚠️ 작업 중 겪은 함정: 미리보기 브라우저 탭이 `style.css`를 강하게 캐싱해서, `navigate{force:true}`로 새로고침해도 CSS 변경이 반영 안 되는 것처럼 보였음(마치 파싱 에러로 규칙이 통째로 사라진 것처럼 오인해 한참 디버깅함) — `<link>`의 `href`에 캐시버스터 쿼리스트링을 붙여서야 최신 CSS가 반영되는 걸 확인함. **다음에 스타일 변경이 브라우저에 반영 안 되는 것처럼 보이면 CSS 문법 문제보다 먼저 이 캐싱 문제부터 의심할 것.**

9. **원형 그래픽 재조정 + 검색실패 화면 개선 — ✅ 완료 (2026-07-25, 같은 세션)**
   - 사용자 피드백으로 까마귀 실루엣 워터마크는 "구리다"며 완전히 제거함(8번 항목에서 새로 추가했던 것을 바로 다음 라운드에 되돌림). 링 자체(무지개빛 그라디언트)는 유지.
   - 링의 "숨쉬는" 펄스 애니메이션이 20s linear라 체감이 거의 안 될 정도로 느렸던 것을 5s ease-in-out으로 교체(스케일 폭도 1.05→1.08로 확대) — 회전은 대칭 그라디언트라 원래 안 보였고, 체감되는 건 스케일 펄스뿐이었다는 걸 재확인.
   - "Launched by Rayben Jo"(엔트리 화면) / "Created from Rayben Jo"(상단 헤더, 검색결과 화면)를 전부 "🚀Launched by JYG"로 통일(로켓 이모지 유지, 헤더 쪽 🔥는 교체).
   - **검색 실패 시 화면이 헤더만 남고 텅 비어 보이던 문제 발견·수정**: 기존엔 토스트(3초)만 뜨고 사라지면 결과카드도 없고 안내도 없어서 "화면이 생략된 것처럼" 보였음(사용자가 실제 스크린샷으로 재현해서 지적함) — `#search-error-state` 블록을 신설해 다음 검색 시작 전까지 "데이터를 불러오지 못했습니다" 안내가 결과카드 자리에 계속 남아있도록 함. 브라우저로 실패(ZZZZ99)/성공(AAPL) 둘 다 실검증 완료.
   - 인트로 인사말을 "AT YOUR SERVICE, SIR"에서 사용자가 바로 "GOOD DAY TODAY :)" / "RAVEN is online"으로 재확정(둘 다 자비스 톤 후보였는데 이쪽이 최종).

10. **실기기 테스트에서 발견된 버그 3종 수정 — ✅ 완료 (2026-07-25, 같은 세션)**
   - 사용자가 실제 안드로이드 폰(Chrome)으로 배포된 사이트를 테스트하며 스크린샷 2장으로 재현 — 지금까지의 모바일 검증은 전부 데스크탑 브라우저의 에뮬레이트 모바일 뷰포트였어서 못 잡던 것들.
   - **안드로이드 화면 확대 버그**: PIN+인사말까지는 정상이다가 티커 입력창이 뜨는 시점(엔트리 화면)에서 레이아웃이 갑자기 확대되며 오른쪽으로 밀려 잘리는 문제. 이전에 고친 건 iOS Safari의 "포커스 시 확대"(input font-size<16px 원인)였는데, 이건 별개로 **안드로이드 Chrome의 "글자 자동 확대(font boosting)"**가 원인으로 추정됨 — `html { text-size-adjust: 100%; -webkit-text-size-adjust: 100%; }` 추가로 대응. 이 툴의 브라우저 프리뷰는 이 동작을 재현 못 해서(데스크탑 크롬 엔진이라) 코드 반영만 하고 실기기 재확인은 사용자 몫.
   - **검색 실패 시 배경색 불일치**: 실패 안내 박스(`#search-error-state`)는 잘 뜨는데 그 뒤 페이지 배경이 검은색 그대로였음 — `raven-result-bg`(보라색 그라디언트) 클래스가 분석 **성공** 시에만 body에 붙던 게 원인. 검색을 시작하는 시점(성공/실패 무관)에 붙도록 이동해서 해결, 브라우저로 실패 케이스 재검증 완료.
   - **투자자별(individual/foreign/institutional) 수급 데이터가 계속 "없음"으로 뜨는 문제**: `interpretSupplyDemand()`의 재수집 조건이 "5종 중 하나라도 있으면 스킵"(`hasAnyData`)이었는데, `investor_trend`를 5번째로 추가하기 전부터 이미 나머지 4종 캐시가 있던 종목은 이 조건 때문에 재수집 자체가 통째로 스킵되어 `investor_trend`만 영원히 안 채워지는 버그였음 — "5종 전부 있어야 스킵"(`hasAllData`)으로 변경해 해결. Node 스크립트로 실제 KIS 호출까지 재현해서 개인/외국인/기관 데이터 정상 수집·해석되는 것 확인함.

11. **RAVEN SCORE 배지 미세조정 — ✅ 완료 (2026-07-25, 같은 세션)**
   - 등급+숫자가 붙어보이던 것(`A+92점`처럼) → `.z-badge-grade sup`에 `margin-left` 추가해 간격 확보.
   - 캡션을 "추세·모멘텀·변동성·R:R·상대강도(RS)를 종합한 자체 점수입니다"에서 "ℹ️ RAVEN 분석에 의한 자체 점수입니다"로 단순화(요청대로 원형 정보 아이콘 사용, ℹ️가 실제로 파란 동그라미+i로 렌더링되는 것 확인).

13. **투자자별 매매동향 "0으로 표시/날짜 오래됨" 버그 수정 — ✅ 완료 (2026-07-30)**
   - 사용자가 종목에 따라 개인/외국인/기관 값이 0으로 나오거나 기준일이 며칠 지난 날짜(예: 7/24)로 나오는 걸 실사용 중 발견해서 보고함.
   - **버그 ①(값 0)**: `investor_trend`는 일중 누적치라 장 시작 직후엔 KIS가 당일 행 자체는 주지만 필드 값이 빈 문자열("")인 상태로 옴(장중 실측으로 재현·확인함). JS에서 `Number("")`는 `NaN`이 아니라 `0`이라, 그냥 `Number()`로 파싱하면 "데이터 없음"이 아니라 "0주 순매수"처럼 없는 데이터를 있는 것으로 잘못 표시하는 버그였음. `parseKisNum()`(빈 문자열/null/undefined를 명시적으로 NaN 처리)을 만들어 5개 해석 함수 전부에 적용 — 이 과정에서 `interpretProgramTrade`만 NaN 가드 자체가 없어서 데이터가 비어도 조용히 "균형" 문구로 새던 것도 추가로 발견해 같이 고침(나머지 4개는 원래 가드가 있었음).
   - **버그 ②(날짜 오래됨)**: `interpretSupplyDemand()`가 "5종 데이터가 다 있으면 재수집 스킵"만 보고 "최신인지"는 안 봐서, 한 번 다 채워진 종목은 그 뒤로 며칠이 지나도 영원히 재수집을 안 하고 있었음 — 캐시 최신 날짜가 오늘로부터 4일 넘게 지났으면 재수집하도록 `daysSince()` 체크 추가. investor_trend 전용으로 "행은 있지만 값이 비어있는" 경우도 별도 유효성 체크(`isInvestorRowUsable`)로 잡아서, 장중 초반이라 아직 값이 안 채워졌으면 그때그때 재시도하도록 함.
   - 실제 KIS 라이브 호출로 재현(종목 259960이 오전 10시46분엔 빈 값, 낮 12시47분엔 실제 값 반환하는 것 확인) → 수정 후 Node 스크립트+브라우저 UI 둘 다로 최종 검증(기준일 2026-07-30, 개인/외국인/기관 실제 수치 정상 표시).

14. **버그 확인 완료(2026-07-26)** — 사용자가 "확인만" 요청한 것 2건, 조사만 하고 코드는 안 건드림:
   - **상단 헤더의 링 애니메이션이 속도 개선 반영 안 됨 — ✅ 발견 즉시 수정 완료(2026-07-26)**: `.raven-smoke-orb`(로그인 전 3개 오버레이 화면용)만 5s ease-in-out으로 고쳤었는데, `top-header::before`(로그인 후 모든 화면, 결과화면 포함 최상단에 항상 보이는 링)는 완전히 별개의 CSS 규칙이라 그 작업에서 빠져있었음 — `.raven-smoke-orb`와 동일한 그라디언트/box-shadow/`animation: ravenRing 5s ease-in-out`으로 맞춰서 해결, 브라우저로 색상 반영 확인함. 두 규칙이 같은 값을 유지해야 한다는 주석을 style.css에 남겨둠(재발 방지).
   - **지표 해석 기준값이 최신(Claude 엔진)인지**: RSI(과열≥70/상승우위≥60/중립/약세≤40/과매도≤30), ADX(뚜렷함≥25/보통/약함<20) 둘 다 Wilder 원조 공식의 표준 임계값과 일치, RS(±5%p)도 기존 확인된 값 그대로 — 전부 2026-07-20 퀀트 업그레이드([[project_raven_trading_app]] 참고, 이때 ADX/MACD크로스/RS 자체가 신규 추가됐음) 때 들어간 최신 기준이 맞음. 임계값 자체는 손댈 필요 없음으로 확인됨. (유가(WTI) 기준값만 "감으로 잡은 값"이라 별도 재검토 필요 — 2026-07-30에 완료, 아래 15번 참고.)

15. **투자자별 연속매매 감지 엔진 + UI 다듬기 배치 — ✅ 완료 (2026-07-30, 같은 세션)**
   - 사용자가 "1번(연속매매 감지)·2번(UI 다듬기 5건)·5번(유가 기준값 재검토)이 간단해 보인다"며 지정, 셋 다 완료.
   - **유가(WTI) 임계값 재검토**: Yahoo에서 5년치(2021~2026) 주봉 실데이터 조회 — p10=$62.7, 중앙값=$76.3, p90=$97.6. 원래 감으로 잡았던 <60/>90이 실제로 하위/상위 10분위와 꽤 근접했던 것으로 확인됨(큰 오류는 아니었음) — 반올림해서 <65/>95로 소폭 조정.
   - **외국인/기관 연속 순매수·순매도 감지 엔진**: `supplyDemandInterpreter.js`에 `computeStreak()`/`interpretInvestorStreak()` 추가 — 최근 15개 `investor_trend` 행을 스캔해서 외국인/기관 각각 며칠 연속 같은 방향인지 + 누적 수량 계산(장중 미집계로 비어있는 "오늘" 행은 건너뛰고 어제부터 셈). 3일 미만은 노이즈성이라 문장 자체를 생략. 실데이터로 검증(크래프톤 259960: "외국인 5일 연속 순매도(누적 46,675주), 기관 5일 연속 순매수(누적 121,460주)"). **범위 축소 결정**: 원래 제안했던 후속 아이디어 5개 중 RAVEN SCORE 반영은 스킵함 — 수급 데이터가 현재 스코어 계산 이후 비동기로 로드되는 구조라, 반영하려면 로딩 순서 자체를 바꿔야 해서 "간단한" 범위를 벗어남(다음에 원하면 별도 작업으로). 텔레그램 알림 연동과 프로그램매매/공매도·대차 조합 신호도 같은 이유로 보류.
   - **UI/엔진 다듬기 5건 중 4건 완료(③ 줄바꿈 정리는 "필수 아님"이라 스킵)**:
     - ① RSI/MACD/ADX/RS/ATR 지표박스에 긍정(녹색)/중립(주황)/부정(빨강) 색상 코딩 — `setIndicatorBox()`에 `sentiment` 파라미터 추가. ADX는 방향(DI+/DI-)이 있어도 추세 자체가 약하면(ADX<20) 신뢰 못 하니 중립 처리, ATR은 원래 방향성 없는 지표라 "점수 감점 요인"이 붙을 때만 부정 색상.
     - ② 문단 안 핵심 문구 강조용 `toneSpan()` 헬퍼 신규 추가, 모멘텀 탭의 "강세/약세 다이버전스" 문구에 적용(스크린샷 예시 그대로).
     - ④ **RAVEN SIGNAL 요약에 MACD 다이버전스가 안 보이던 게 진짜 버그로 확인됨** — ADX/MACD크로스/RS는 이미 근거 문구에 들어가는데 다이버전스만 빠져있었음. BUY(지지선 근처)/SELL(저항선 근처)/SELL(R:R 불리) 세 분기에 전부 추가.
     - ⑤ RAVEN SCORE의 "점" 단위 글자가 숫자/등급과 따로 흰색으로 남아있던 것 — 같은 색으로 통일(별도 sibling 엘리먼트라 id 추가해서 색 지정).
   - ⚠️ **검증 중 겪은 심각한 캐싱 문제**: 이번엔 `localhost:5500`이 서버 재시작 + 새 탭을 만들어도 계속 이전 코드를 서빙하는 걸 발견(curl로 서버가 최신 파일을 정확히 주고 있는 것까지 확인했는데도 브라우저만 안 바뀜) — `http://127.0.0.1:5500`(다른 origin 취급)으로 접속하니 캐시가 완전히 우회됨. 다만 이러면 로컬 백엔드 CORS(`FRONTEND_ORIGIN`)에 안 걸려있어서 확인 끝나고 원복하는 조건부로 `.env`에 잠깐 추가했다가 검증 후 되돌림. **다음에도 스타일/스크립트 변경이 브라우저에 전혀 반영 안 되면(재시작·새 탭도 무소용) `127.0.0.1`로 접속해볼 것.**

16. **다음 우선순위 후보 (2026-07-31 갱신, 아직 미정, 세션 시작 시 사용자와 정하기)**
   - **연속매매 신호 후속 확장** — RAVEN SCORE 반영, 텔레그램 알림 연동, 프로그램매매/공매도·대차 조합 신호(위 15번 참고, 로딩 순서 문제로 이번엔 스킵함)
   - Phase 5(뉴스+실적 그래프) — 토스증권 UI 스크린샷을 디자인 레퍼런스로 채택함([[project_raven_trading_app]] 참고)
   - Phase 6(국내장 프리마켓 분석 + 익일 신규진입 추천 — 규모 큰 작업)
   - 관심종목 UI 확장(사이드바/전용 페이지) — 아래 Phase 3 항목 참고

17. **수급 5종 중 "당일 실시간" 3종(프로그램매매/공매도/투자자별)이 당일 데이터를 못 불러오던 근본 원인 수정 — ✅ 완료 (2026-07-31)**
   - 사용자가 "프로그램매매·투자자별 매매동향이 당일 데이터 기준으로 안 불러와지거나 없다고 표시된다"고 보고해서 재조사함.
   - **1차 원인**: `program_trade`/`investor_trend`는 KIS가 일중 실시간 누적치로 주는 데이터라, 장중엔 오늘 날짜 행 자체는 있어도 값이 빈 문자열로 옴 — 기존 코드는 "DB에서 최신 날짜 행 딱 하나"만 보고 해석해서, 그 행이 비어있으면 어제자 정상 데이터가 캐시에 있어도 그냥 "데이터가 없습니다"로 표시했음. `program_trade`는 이 빈 값 자체를 감지하는 가드도 없었음. → 최근 5개 행 중 값이 채워진 가장 최신 행으로 자동 폴백하도록 수정.
   - **2차 발견(사용자 질문 "다른 항목도 당일 데이터 불러오는 거 아니냐"에 답하며 실측 재검증)**: 실제 KIS 응답을 떠보니 `short_sale`(공매도)도 `program_trade`/`investor_trend`와 동일하게 당일 실시간 누적 데이터였음(장마감 직후 실측 시 당일 값이 이미 채워져 있는 것 확인) — 이 셋이 진짜 "당일 데이터"를 주는 3종이고, `credit_balance`(신용잔고, 실측상 3일 지연)·`loan_trans`(대차거래, 1일 지연)는 KIS가 완성된 날짜만 돌려줘서 애초에 빈 당일행 문제가 없음. `short_sale`에도 동일한 폴백 로직 적용.
   - **가장 근본적인 3차 버그(재수집 트리거 자체의 결함)**: DB를 직접 열어보니 "어제 데이터가 정상적으로(빈 값 아니게) 캐시돼 있으면" 재수집 트리거(`hasAllData`/`isStale`) 자체가 아예 안 걸려서, 오늘 데이터를 단 한 번도 시도조차 안 해보고 하루 종일 어제 데이터만 보여주고 있었음(장마감 후에도 마찬가지 — 실제로 16:00 KST에 재현·확인함). "캐시된 값이 채워져 있는지"뿐 아니라 **"그 값이 오늘 날짜인지"**까지 같이 봐야 했던 것. `todayKST()` + `isFreshForToday()`로 캐시된 값이 오늘 날짜가 아니면 무조건 재시도하도록 수정 — 이게 사용자가 겪은 문제의 진짜 근본 원인이었음.
   - `server/src/lib/supplyDemandInterpreter.js` 수정, 삼성전자(005930)·크래프톤(259960) 실KIS 호출로 검증 완료(수정 전: 캐시된 전날짜 데이터만 반환 / 수정 후: 두 종목 다 당일 실제 수치로 정상 반환, 재요청 시 0.4초 만에 캐시 히트되는 것도 확인해 불필요한 KIS 재호출 없음을 확인함).

18. **UI 다듬기 ③ 줄바꿈 정리 — ✅ 완료 (2026-07-31)**
   - 브라우저에서 375px 모바일 폭 기준으로 결과 화면 전체를 실측(좌표 기반 스캔)해서 진짜 줄바꿈 버그 2건 발견·수정:
     - `app.js`의 상단 요약 줄(`#main-comment`) — "▼ DOWN: 22.0%"가 "DOWN:"까지만 1번째 줄에 남고 "22.0%"만 2번째 줄로 떨어져 나가는 버그(2026-07-25 RS 표기 버그와 같은 유형). 통계 문구별로 `white-space:nowrap` 적용해 문구 단위로만 줄바꿈되도록 수정.
     - `style.css`의 CHECK POINT 표(지지/저항 5칸 그리드) — "2차 지지"/"2차 저항"처럼 값이 없는(`—`) 칸은 그리드 열이 12px까지 쪼그라들면서 라벨 글자가 세로로 한 글자씩 쌓이는 기형적 렌더링이 실제로 나고 있었음(CJK는 글자 사이 어디서나 줄바꿈이 허용돼서 `1fr` 그리드 트랙이 극단적으로 좁아질 수 있었던 것). `.wave-chip-label`에 `white-space:nowrap` 적용해 해결.
     - 그 외 서술형 문단(수급/신호/패턴 코멘트 등)의 줄바꿈은 한글 자연 줄바꿈(음절 단위)이라 정상 — 손대지 않음. RS/ATR 지표줄도 375px에서 이미 한 줄에 들어가는 것 확인함.
   - ⚠️ 320px 같은 초소형 구형 화면에서는 라벨 고정폭 때문에 CHECK POINT 표가 넘칠 수 있음(실측 확인) — 이 프로젝트의 실사용 기준 최소 폭은 375px(iPhone SE)라 범위 밖으로 보고 손대지 않기로 함.
   - 검증 중 겪은 캐싱 함정: `localhost:5500`이 이번에도 `app.js`/`style.css`를 강하게 캐싱해서 수정이 반영 안 되는 것처럼 보였음 — `127.0.0.1:5500`으로 우회해서 확인함(과거에도 겪은 문제, [[project_raven_trading_app]] 참고). 확인 중 로컬 백엔드 CORS(`FRONTEND_ORIGIN`)에 `127.0.0.1:5500`을 임시로 추가했다가 검증 후 원복함.

19. **텔레그램 실제 발송 테스트 — ✅ 완료 (2026-07-31)**
   - `POST /api/watchlist/check-now-and-alert`를 사용자 동의 받고 실제 트리거함. 관심종목 5종(005930/034020/009150/082740/058610) 전체 신호 체크 — 058610(BUY, 거래량 급증+상승), 082740(SELL, 거래량 급증+하락) 2건에서 실제로 텔레그램 메시지 정상 발송됨(에러 없이 처리, 사용자 확인 요청함). Phase 3에서 미착수로 남겨뒀던 마지막 항목 완료.

### Phase 3. 매수/매도 타점 시그널 + 관심종목 — ✅ 완료 (2026-07-20, 커밋 `8a1a59c`)
- [x] 골든크로스/데드크로스 감지 로직 — `server/src/lib/signalDetector.js`의 `detectMACross()`. MA20/MA60 EMA가 "어제→오늘 사이에" 대소관계가 뒤집힌 경우만 감지(상태가 아니라 사건 기준이라 매일 반복 알림 안 됨). 합성 데이터로 크로스 당일=GOLDEN, 다음날=NONE 정확히 검증함.
- [x] 거래량 급증 + 방향 결합 신호 — `detectVolumeSurge()`. 평균 대비 2배 이상 거래량 + ±1.5% 이상 당일 등락이 같이 나온 날만 BUY/SELL. 매도 신호(데드크로스·거래량급증매도)가 항상 우선하는 리스크 관리 우선 정책.
- [x] 관심종목 DB 등록 — `server/src/routes/watchlist.js`(GET/POST/DELETE `/api/watchlist`), 프론트에 별표 토글 버튼(결과화면)+관심종목 pill 목록(헤더) 추가. `server/db/schema.sql`에 `watchlist` 테이블 추가, 사용자가 Supabase SQL 에디터에서 직접 실행 완료. add×2/list/delete×2/빈 목록까지 실제 DB로 라운드트립 검증함.
- [x] 스케줄러 → 텔레그램 알림 — `server/src/scheduler.js`가 매일 06:00 KST에 관심종목 전체(국내+해외)를 돌면서 신호 체크 후 `server/src/lib/telegram.js`로 전송. 수동 테스트용 `POST /api/watchlist/check-now-and-alert`(실제 전송됨), `GET /api/watchlist/check-now/:symbol`(판정만, 전송 안 함) 둘 다 마련해둠.
- [x] 실제 텔레그램 전송 테스트 완료(2026-07-31, 위 19번 참고) — `check-now-and-alert` 실행해 058610/082740에서 실제 발송 확인. 관심종목은 실사용 등록 시작함(2026-07-21, 058610).
- 📌 (2026-07-21 추가) 관심종목 UI 확장 필요 — 지금은 헤더에 가로 pill 목록으로만 보여주는데, 종목 수가 늘어나면 한 줄에 다 안 들어옴. 왼쪽 사이드바로 열어서 목록을 보여주거나, 관심종목 전용 페이지(예: `/watchlist`)를 만드는 방향으로 확장 예정. UI/라우팅 구조를 새로 짜야 해서 별도 작업 단위로 진행.

### Phase 4. 실제 AI 분석 연동 — ✅ 완료 (2026-07-23)
- [x] `@anthropic-ai/sdk` 설치 + `server/src/lib/anthropicClient.js`(API 키 lazy 초기화, `ANTHROPIC_API_KEY` 미설정 시 명확한 에러)
- [x] `POST /api/ai/analyze` (`server/src/routes/ai.js`) — 프론트가 이미 계산해둔 지표(RSI/MACD/ADX/ATR/RS/지지·저항/목표가·손절가/캔들패턴/KIS 수급코멘트)를 그대로 JSON으로 받아 프롬프트를 구성 → Claude Sonnet 5(`claude-sonnet-5`)에게 한국어 서술 분석 요청 → 텍스트 반환. 서버가 숫자를 재계산하지 않고 프론트 계산 결과만 그대로 서술하도록 시스템 프롬프트에 명시(할루시네이션 방지)
- [x] 결과 화면에 "🤖 AI 분석 요청" 버튼 추가 (목표가 박스 아래, 탭 위) — **버튼 클릭 시에만 호출**(비용 통제 목적, 자동 실행 안 함). 클릭 시 `lastAnalysis` 전역 상태(마지막 분석 결과 캐시)를 그대로 페이로드로 전송
- [x] 모델은 Claude Opus 4.8(스킬 기본값) 대신 **Claude Sonnet 5**로 사용자가 직접 선택 — 서술 분석은 Opus급 정밀도가 필요한 작업이 아니고, 버튼 클릭마다 과금되는 구조라 비용 대비 품질 균형을 우선함
- [x] 실제 API 키(`console.anthropic.com` 발급)로 종단 검증 완료 — AAPL 샘플 지표로 curl 테스트 시 4문단짜리 자연스러운 한국어 서술(추세/수급/패턴/리스크 요약) 정상 반환 확인. 브라우저에서도 버튼 클릭 → 요청 → 로딩 상태 → 결과 표시/에러 처리 전체 경로 검증
- API 키는 `server/.env`에만 보관(프론트 노출 없음), git 제외 확인됨

### Phase 5. 뉴스 + 실적 그래프
- [ ] DART API(국내) + Yahoo(해외) 연동
- [ ] 분기별 매출/영업이익 막대그래프, 실적발표일 표시
- [ ] 뉴스 검색 연동 (국내: 네이버뉴스 / 해외: 별도 뉴스 API)

### Phase 6. 국내장 프리마켓 분석 + 익일 신규진입 추천 (2026-07-20 사용자 요청, 규모 큰 작업이라 별도 Phase로 분리)
- [ ] 전일 밤 미국/해외 지수 흐름 수집: 나스닥종합, 나스닥선물, 필라델피아반도체지수(SOX), S&P500, S&P500선물, 코스피 야간선물, 달러(환율/달러인덱스)
  - 📌 데이터 소스 리서치 필요 — 나스닥/SOX/S&P500 계열은 Yahoo Finance(매크로 지표와 같은 패턴, `^IXIC`/`^SOX`/`^GSPC`/`ES=F`/`NQ=F` 등)로 될 가능성 높음. **코스피 야간선물(CME 상장)은 별도 확인 필요** — Yahoo에 정확한 티커가 있는지, 없으면 다른 소스를 찾아야 함
- [ ] 위 지수 흐름을 종합해 "오늘 국내장 전체 분위기 예상" 서술 생성 (상승 개장 예상/하락 개장 예상/혼조 등)
- [ ] Phase 2~3에서 이미 구축된 수급 4종(프로그램매매·공매도·신용잔고·대차거래) 데이터와 결합
- [ ] 관심종목(국내) 대상으로 "다음날 오전 신규 진입 추천" 코멘트 자동 생성 — 스케줄러가 새벽 수급 수집 이후 이어서 실행하는 방식이 자연스러울 것(기존 06:00 KST 잡과 같은 흐름)
- [ ] 텔레그램 알림에 이 프리마켓 분석도 포함할지, 별도 메시지로 분리할지는 구현 시 결정

### Phase 7. 마무리
- [ ] 반응형/PWA 다듬기 → 이후 앱(iOS/Android) 전환 논의

---

## 7. 제안했던 추가 개선 아이디어 (여유 있을 때 검토)

"진짜 트레이더 방식"을 위한 지표 추가 제안:
- **VWAP** (거래량가중평균가) — 기관 매매 기준선
- **Volume Profile** (거래량 프로파일) — 가격대별 거래량 분포로 지지/저항 재검증
- **상대강도(RS)** — 개별 종목 vs 지수/섹터 대비 강도
- **ATR 기반 손절** — 변동성 기반 손절 병행
- **멀티 타임프레임 분석** — 일봉 외 주봉/시간봉 병행

---

## 8. 작업 시 주의사항

- 사용자는 코딩 초심자 → 새로운 개념/명령어는 왜 필요한지, 무엇을 하는지 쉽게 설명하고 진행
- API 키, PIN 등 민감정보는 절대 프론트엔드(클라이언트 JS)에 노출하지 말 것 — 반드시 서버 사이드에서만 처리
- 기존 코드의 디자인/UX(PIN-인트로-엔트리 흐름, 스모크 배경 애니메이션 등)는 유지하면서 기능만 확장하는 방향
- CFD 관련 기능은 요청 범위에서 제외됨 (구현 시도 불필요)
